{
  "name": "digitalstudycenter-app",
  "version": "1.0.0",
  "description": "DigitalStudyCenter Mobile App",
  "scripts": {
    "build": "node build.js",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "android": "node build.js && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.0.0",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3"
  }
}
